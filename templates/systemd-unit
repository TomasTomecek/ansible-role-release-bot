[Unit]
Description=Release-bot in a container ({{ container_name }})

[Service]
Restart=always
ExecStart=/usr/bin/podman run --rm --name {{ container_name }} {{ image_name }}
ExecStop=/usr/bin/podman stop -t 10 {{ container_name }}
KillMode=process

[Install]
WantedBy=multi-user.target

